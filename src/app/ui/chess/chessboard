export default function Chessboard() {
    const squares:string[] = [];
    const black = "black"
    for (let i = 0; i < 64; i++) {
        if ( i % 2 === 0) {
            
            squares.push("white")
        }
    }
    return (
        
    )
}